{
  "name": "@shared-utils/utils",
  "version": "0.69.2",
  "description": "Core utilities (log, turnstile, options-manager)",
  "type": "module",
  "engines": {
    "node": "^22.18.0 || >=24.11.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./*": "./dist/*"
  },
  "scripts": {
    "build": "rm -f tsconfig.tsbuildinfo && (yarn --version > /dev/null 2>&1 && yarn tsc --outDir ../dist/utils || npx tsc --outDir ../dist/utils)",
    "build:clean": "rm -f tsconfig.tsbuildinfo && rm -rf ../dist/utils && yarn build",
    "test": "yarn build && NODE_OPTIONS=\"--experimental-vm-modules\" jest",
    "test:watch": "yarn build && NODE_OPTIONS=\"--experimental-vm-modules\" jest --watch",
    "dev": "npx tsc --outDir ../dist/utils --watch"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "express": "^5.2.1",
    "ioredis": "^5.11.1",
    "lodash-es": "^4.18.1",
    "sanitize-html": "^2.17.5"
  },
  "devDependencies": {
    "@babel/core": "^8.0.1",
    "@babel/preset-env": "^8.0.1",
    "@babel/preset-typescript": "^8.0.1",
    "@types/express": "^5.0.6",
    "@types/ioredis": "^5.0.0",
    "@types/jest": "^30.0.0",
    "@types/lodash-es": "^4.17.12",
    "@types/node": "^25.9.3",
    "@types/sanitize-html": "^2.16.1",
    "babel-jest": "^30.4.1",
    "jest": "^30.4.2",
    "typescript": "^6.0.3"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
